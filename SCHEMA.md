# 資料格式說明（SCHEMA）

## 兩條軌道

課程分兩種來源，網頁選單分成「📘 文法課」「💬 會話課」兩區：

- **文法課（依冊）**：放 `data/lessons/`，檔名 `B<冊>L<課>.yaml`（如 B3L02）。有 `book`+`lesson`。
- **會話課（依主題）**：放 `data/kaiwa/`，一個文法一個檔。需要 `track: 会話`、`topic`（唯一 id）、
  `order`（排序）、`label`（選單短名，如「んです」）、`title`。其餘欄位（intro/goals/grammar/vocab）同下。
  範例見既有的 `data/kaiwa/ndesu.yaml`。

> 匯入新內容時先講明這次是「文法」還是「會話」：文法→`data/lessons/B?L??.yaml`；會話→`data/kaiwa/<主題>.yaml`。

---

每一課是一個 YAML 檔，放在 `data/lessons/`（文法課）或 `data/kaiwa/`（會話課）。
「來學日本語」是多冊系列、每冊各從第1課編到第10課，所以用**依冊編號**：
檔名 `B<冊>L<課>.yaml`，例如 `B1L01.yaml`（第一冊第1課）、`B3L03.yaml`（第三冊第3課）。
照片也放對應的 `raw/B1L01/`。

所有複習材料（Anki 牌組、網頁）都由腳本從這些 YAML 自動生成，**資料只有這一份來源**。

## 欄位

```yaml
book: 3                 # 第幾冊（整數，必填）
lesson: 3              # 該冊第幾課（整數，必填）
title: 第三冊 第3課     # 課名（必填）

intro: |               # 導讀／說明（必填）— 一進這課先讀，快速進入狀況
  這一課在學什麼、主題是什麼、各文法之間怎麼連貫、初學要注意的概念。
  可分多段（空一行分段）。

goals:                 # 學習目標（必填）— 學完這課應該會做的事
  - "用 ～ 表達 ～"
  - "分辨 A 和 B"

grammar:                # 文法（選填）— 依講義「文型」結構呈現
  - point: "文型一　句型名稱"   # 文型標題（盡量保留講義的「文型一/二…」編號）
    source: 文法         # 選填：文法 / 会話 / 文法・会話（哪一堂課教的）
    key: true           # 選填：標為重點（常考、兩堂課都教過）
    setsuzoku:          # 接続（選填）：變化／接續規則，逐條列。對應講義的【接続】框
      - "I類（五段）：語尾う段 → お段 ＋ う"
      - "II類（一段）：去る ＋ よう"
    explain: "說明（對應講義【説明】，可多段）"
    table:              # 圖解表格（選填）— 顯示在説明與例之間
      caption: "標題（選填）"
      headers: ["欄1", "欄2", "欄3"]
      rows:
        - ["{{会|あ}}う", "{{会|あ}}[い]ます", "促音便"]   # 每格可用標記：
        - ["{{書|か}}く", "{{書|か}}[い]て", "イ音便"]
      # cell 標記：[x]＝紅色標出變化處；{{漢字|假名}}＝ruby 注音
    examples:           # 例（對應講義【例】的示範句）
      - jp: "日文例句"
        kana: "假名讀音"   # 漢字注音，背誦/朗讀用
        zh: "中文翻譯"
    practice:           # 練習（選填）：講義原本的練習題，盡量保留原編號
      - q: "① さあ、帰りましょう →"   # 題目（含空格＿＿或→）
        a: "帰ろう"                   # 答案
        note: "或：…（替代解答／類別提示）"  # 選填

vocab:                  # 單字（選填）
  - jp: "信号"          # 寫法（漢字或假名）
    kana: "しんごう"     # 讀音
    zh: "紅綠燈"         # 中文意思
    pos: "名詞"          # 詞性（名詞／動詞I/II/III／い形容詞／な形容詞…）
    source: 会話         # 選填：來源（同上）
    key: true           # 選填：重點
    note: "補充說明"      # 選填
    ex:                 # 選填：例句（單字卡「全部單字」清單會顯示）
      jp: "信号を渡ります。"
      kana: "しんごうを わたります。"
      zh: "看紅綠燈過馬路。"

exercises:             # 練習題（選填）
  - type: fill         # fill 填空 / translate 翻譯 / choice 選擇
    q: "題目"
    a: "答案"
    explain: "解析"     # 選填

notes:                 # 老師補充、易錯重點（選填，字串清單）
  - "重點一"
  - "重點二"
```

## 慣例

- 缺的欄位可省略，不要留空字串。
- **每課都要有 `intro` 和 `goals`**（導讀＋學習目標），讓人能快速進入狀況。
- **以講義為主還原結構**：若有老師講義，文法要照講義的「文型」順序與編號編排，
  用 `setsuzoku`（接続）＋`explain`（説明）＋`examples`（例）＋`practice`（練習）完整重現該頁版面，
  讓人讀起來就像在看講義、能回想上課內容。練習題盡量保留講義原本的編號與題目。
- 文法的 `explain` 要寫足夠：概念、接續、與相近文法的差別、初學易錯點。
- **`kana` 一律補上**，Anki、朗讀、以及**漢字注音**都靠它（見下方〈漢字注音規範〉）。
- **所有日文漢字都要盡可能標上平假名發音**；使用者可在「設定 → 假名顯示方式」自行調整或關閉。
  絕大多數是自動的，你只要把 `kana` 寫正確即可。詳見〈⭐ 漢字注音（ふりがな）規範〉。
- 跨課的主題總整理（例如「所有て形用法」）放 `data/decks/`，格式相同但 `book`/`lesson` 可省略、改用 `title`。

## ⭐ 漢字注音（ふりがな）規範

> **總原則：專案裡所有日文漢字都要盡可能標上平假名發音。**
> 使用者若不需要，可在「設定 →**假名顯示方式**」自行關閉或改成整句假名。

### 使用者端：三種顯示方式（一個設定管全站）

| 設定值 | 例句・單字・文章 | 練習題・接続・標題・圖解表格 |
|---|---|---|
| `line` 整句假名在下（**預設**） | 顯示 `kana` 那一行 | 顯示漢字上方注音 |
| `ruby` 漢字上方注音 | 漢字上方注音 | 漢字上方注音 |
| `off` 不顯示假名 | 都不顯示 | 都不顯示 |

- 練習題／接続／標題／表格**沒有「整句假名」可放**，所以在 `line` 模式也顯示注音。
- ⚠️ **測驗頁一律不注音**，**單字卡正面也不注音**（翻面後才顯示）——否則直接洩題。
- TTS 一律讀資料欄（`kana`／`jp`），不讀畫面，所以注音不會被唸兩次。

### 作者端：你只要寫對 `kana`，注音是自動的

**有 `kana` 欄的地方（例句／單字／文章句）→ 什麼都不用做。**
build 會用 `jp` × `kana` 自動對齊算出每個漢字的讀音，產生 `_ruby`。

自動化的兩道安全規則（**不確定就不標，絕不標錯**）：

1. **切法唯一才標**：`{{子|こ}}どもたちは…` 這種只有一種切法的，直接採用。
2. **有歧義時用佐證裁決**：例如「人と人」可以切成 `人=ひ/人=とひと` 或 `人=ひと/人=ひと`，
   會拿全專案已確定的讀音當佐證，只有在「唯一一種切法的每個讀音都有佐證」時才採用。

仍標不到的（多為數字、熟字訓），就**在 `jp` 直接手寫** `{{漢字|假名}}`：

```yaml
- {jp: "{{息子|むすこ}}はもう{{20歳|はたち}}です。", kana: "むすこは もう はたちです。", zh: "兒子已經20歲了。"}
```

> build 會原樣沿用你的標記，並把 `jp` 洗回無標記的乾淨文字（測驗／Anki／TTS 用那個）。

### 沒有 `kana` 欄的欄位（練習題 `q`/`a`、`setsuzoku`、`point`、表格）

這些**也會自動注音**（存成 `_rq`/`_ra`/`_rpoint`/`_rsetsuzoku`），讀音來源是
①全專案例句學到的字典 ②`data/readings.yaml` 人工補充。

規則比上面更嚴：**整段漢字必須完整命中字典才標，絕不逐字硬拆**。
因為這些欄位是**中日混排**，逐字拆會把中文標成日文音
（例：「前面用辭書形還是」會被拆成「前(まえ)面用…」）。
**所以未標的多半是中文敘述，那是正確行為，不用去「補滿」。**

另有兩道保護：
- **量詞**：數字後面的 時/人/日/月/分/本/回/個 讀音會變（1本＝いっぽん），不確定就不標。
- **防洩題**：`is_reading_quiz()` 偵測「考怎麼唸」的題目（含「唸法」「読み方」等），**題目不標**。

### `data/readings.yaml`：人工補充讀音

當 build 印出的注音數字不理想、或發現誤標時，在這裡加一行修正。三種鍵：

```yaml
"隣": となり          # 漢字段 → 讀音（讀音唯一的詞）
"出|さ": だ           # 漢字段|送假名 → 讀音（多讀音字必須用這種，才能消歧）
"#時": じ             # #漢字 → 數字後面（量詞）的讀音
```

優先順序：`#量詞` → 語料的送假名context → `readings.yaml`（帶送假名）→ `readings.yaml` → 語料唯一讀音。
所以 `readings.yaml` 可以覆蓋自動學到的錯誤讀音。

> 💡 **多讀音又無法用送假名區分的字（間＝あいだ/ま、時＝とき/じ、家＝いえ/うち）故意不列**——
> 寧可不標，也不要標錯。

### 說明類欄位（`explain`／`intro`／`goals`／`notes`）

這些是**中文說明**，不自動注音。若要替其中的日文加注音，**手寫** `{{漢字|假名}}` 即可
（這些欄位也支援 `**粗體**`）。同樣受「假名顯示方式」設定控管。

### 加完內容後一定要確認

跑 `python scripts/build.py`，看結尾這兩行：

```
🔤 漢字注音：… 仍有歧義 0／對不齊 0 … → 覆蓋率 100.0%
📝 練習題／接続／標題／表格：已標 N 段、未標 M 段
```

- **「對不齊」＞0**：⚠️ 幾乎都代表 `kana` 寫錯了，**優先修**（曾靠它抓到多筆錯字）。
- **「仍有歧義」＞0**：用 `{{漢字|假名}}` 手動補在 `jp` 上。
- **覆蓋率要維持 100%**。

## 文法課 + 會話課（同一課兩個來源）

一堂課分文法課與會話課，會話課也教文法、進度略不同，但都貼在同一個 `B?L??.yaml`。

- `source`：標來源。文法課 `文法`、會話課 `会話`、兩堂都教過 `文法・会話`。
- **自動合併**：同一文法／單字重複出現時，只留一條，`source` 標 `文法・会話`、`key: true`（重複＝重點）。
  例句若不同則一起保留在 `examples`。
- Anki：`source` 會拆成 `文法`/`会話` 標籤，`key` 會加 `重点` 標籤，可在 Anki 篩選複習。

## 新增一課的流程

1. 把照片放到 `raw/B3L04/`（第三冊第4課）
2. 告訴 Claude「匯入第三冊第4課」，由 Claude 辨識照片、整理成 `data/lessons/B3L04.yaml`
3. 執行 `python scripts/build.py` 重新生成複習材料
