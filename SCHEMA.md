# 資料格式說明（SCHEMA）

## 兩條軌道

課程分兩種來源，網頁選單分成「📘 文法課」「💬 會話課」兩區：

- **文法課（依冊）**：放 `data/lessons/`，檔名 `B<冊>L<課>.yaml`（如 B3L02）。有 `book`+`lesson`。
- **會話課（依主題）**：放 `data/kaiwa/`，一個文法一個檔。需要 `track: 会話`、`topic`（唯一 id）、
  `order`（排序）、`label`（選單短名，如「んです」）、`title`。其餘欄位（intro/goals/grammar/vocab）同下。
  範例見既有的 `data/kaiwa/ndesu.yaml`。

> 匯入新內容時先講明這次是「文法」還是「會話」：文法→`data/lessons/B?L??.yaml`；會話→`data/kaiwa/<主題>.yaml`。

---

每一課是一個 YAML 檔，放在 `data/lessons/`（文法課）或 `data/kaiwa/`（會話課）。
「來學日本語」是多冊系列、每冊各從第1課編到第10課，所以用**依冊編號**：
檔名 `B<冊>L<課>.yaml`，例如 `B1L01.yaml`（第一冊第1課）、`B3L03.yaml`（第三冊第3課）。
照片也放對應的 `raw/B1L01/`。

所有複習材料（Anki 牌組、網頁）都由腳本從這些 YAML 自動生成，**資料只有這一份來源**。

## 欄位

```yaml
book: 3                 # 第幾冊（整數，必填）
lesson: 3              # 該冊第幾課（整數，必填）
title: 第三冊 第3課     # 課名（必填）

intro: |               # 導讀／說明（必填）— 一進這課先讀，快速進入狀況
  這一課在學什麼、主題是什麼、各文法之間怎麼連貫、初學要注意的概念。
  可分多段（空一行分段）。

goals:                 # 學習目標（必填）— 學完這課應該會做的事
  - "用 ～ 表達 ～"
  - "分辨 A 和 B"

grammar:                # 文法（選填）— 依講義「文型」結構呈現
  - point: "文型一　句型名稱"   # 文型標題（盡量保留講義的「文型一/二…」編號）
    source: 文法         # 選填：文法 / 会話 / 文法・会話（哪一堂課教的）
    key: true           # 選填：標為重點（常考、兩堂課都教過）
    setsuzoku:          # 接続（選填）：變化／接續規則，逐條列。對應講義的【接続】框
      - "I類（五段）：語尾う段 → お段 ＋ う"
      - "II類（一段）：去る ＋ よう"
    explain: "說明（對應講義【説明】，可多段）"
    examples:           # 例（對應講義【例】的示範句）
      - jp: "日文例句"
        kana: "假名讀音"   # 漢字注音，背誦/朗讀用
        zh: "中文翻譯"
    practice:           # 練習（選填）：講義原本的練習題，盡量保留原編號
      - q: "① さあ、帰りましょう →"   # 題目（含空格＿＿或→）
        a: "帰ろう"                   # 答案
        note: "或：…（替代解答／類別提示）"  # 選填

vocab:                  # 單字（選填）
  - jp: "信号"          # 寫法（漢字或假名）
    kana: "しんごう"     # 讀音
    zh: "紅綠燈"         # 中文意思
    pos: "名詞"          # 詞性（名詞／動詞I/II/III／い形容詞／な形容詞…）
    source: 会話         # 選填：來源（同上）
    key: true           # 選填：重點
    note: "補充說明"      # 選填
    ex:                 # 選填：例句（單字卡「全部單字」清單會顯示）
      jp: "信号を渡ります。"
      kana: "しんごうを わたります。"
      zh: "看紅綠燈過馬路。"

exercises:             # 練習題（選填）
  - type: fill         # fill 填空 / translate 翻譯 / choice 選擇
    q: "題目"
    a: "答案"
    explain: "解析"     # 選填

notes:                 # 老師補充、易錯重點（選填，字串清單）
  - "重點一"
  - "重點二"
```

## 慣例

- 缺的欄位可省略，不要留空字串。
- **每課都要有 `intro` 和 `goals`**（導讀＋學習目標），讓人能快速進入狀況。
- **以講義為主還原結構**：若有老師講義，文法要照講義的「文型」順序與編號編排，
  用 `setsuzoku`（接続）＋`explain`（説明）＋`examples`（例）＋`practice`（練習）完整重現該頁版面，
  讓人讀起來就像在看講義、能回想上課內容。練習題盡量保留講義原本的編號與題目。
- 文法的 `explain` 要寫足夠：概念、接續、與相近文法的差別、初學易錯點。
- `kana` 一律補上，Anki 和朗讀都會用到。
- 跨課的主題總整理（例如「所有て形用法」）放 `data/decks/`，格式相同但 `book`/`lesson` 可省略、改用 `title`。

## 文法課 + 會話課（同一課兩個來源）

一堂課分文法課與會話課，會話課也教文法、進度略不同，但都貼在同一個 `B?L??.yaml`。

- `source`：標來源。文法課 `文法`、會話課 `会話`、兩堂都教過 `文法・会話`。
- **自動合併**：同一文法／單字重複出現時，只留一條，`source` 標 `文法・会話`、`key: true`（重複＝重點）。
  例句若不同則一起保留在 `examples`。
- Anki：`source` 會拆成 `文法`/`会話` 標籤，`key` 會加 `重点` 標籤，可在 Anki 篩選複習。

## 新增一課的流程

1. 把照片放到 `raw/B3L04/`（第三冊第4課）
2. 告訴 Claude「匯入第三冊第4課」，由 Claude 辨識照片、整理成 `data/lessons/B3L04.yaml`
3. 執行 `python scripts/build.py` 重新生成複習材料
